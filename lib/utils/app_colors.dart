import 'package:flutter/material.dart';

class AppColors {
  // Primary color used throughout the app into main buttons and active elements
  static const Color primary = Color(0xFF1E88E5);
  // Background color for the app's scaffold and main surfaces
  static const Color background = Color(0xFFF9FAFB);
  // Dark text color for primary text elements
  static const Color textDark = Color(0xFF212121);
  // Light text color for secondary text elements
  static const Color textLight = Color(0xFF9E9E9E);
  // Accent color used into splash screens and highlights
  static const Color accent = Color(0xFF98B3E1);

  static  const Color error = Color(0xFFD32F2F);
}
